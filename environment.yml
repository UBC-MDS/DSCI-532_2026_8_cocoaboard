# CocoaBoard – development only (minimal; conda resolves the rest)
name: cocoaboard
channels:
  - conda-forge
dependencies:
  - python=3.12.12
  - pip=26.0.1

  # App
  - shiny=1.4.0
  - pandas=3.0.0
  - altair=6.0.0

  # Notebooks / dev
  - jupyterlab=4.5.4
  - ipykernel=7.2.0
