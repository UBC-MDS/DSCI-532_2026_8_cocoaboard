# CocoaBoard runtime dependencies (PyPI-only; no conda paths).
# Versions match environment.yml. For local dev, use: conda env create -f environment.yml
# Do not use pip freeze from a conda env—it embeds local paths and breaks publishing.

shiny==1.4.0
pandas==3.0.0
altair==6.0.0
